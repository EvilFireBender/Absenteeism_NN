preprocessing:

1) dropping id column
2) one-hot-encoding reason for absence
3) removing reason for absence, and adding its one-hot-encoded version in its place
4) normalising all data except target column
5) changing target to three categories